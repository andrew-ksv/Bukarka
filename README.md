# Automated tests for Bukarka online book store

## Description
Automated frontend tests for Bukarka online book store, built with Cypress 🌲 JavaScript.

## Installation
1. Clone the repository
2. Navigate to the project directory
3. Install dependencies: npm install

## Running Tests
- **Run all Cypress tests**: npx cypress run
- **Run a specific Cypress test**: npx cypress run --spec cypress/e2e/test-name.cy.js
- **Run Cypress in UI mode**: npx cypress open